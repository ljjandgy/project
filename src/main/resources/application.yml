server:
  port: 80

spring:
  datasource:
    name: test
    url: jdbc:mysql://127.0.0.1:3306/test
    username: root
    password: root
  thymeleaf:
    enabled: false
  mvc:
    view:
      static-path-pattern: /**
  resources:
    static-locations: classpath:/views/,classpath:/static/
